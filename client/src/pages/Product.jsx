import React from 'react';
import ProductView from '../components/ProductView';
const Product = (id) => {
  return (
    <div>
      <ProductView id ={id} />
    </div>
  );
};

export default Product;
